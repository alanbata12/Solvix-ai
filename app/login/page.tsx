"use client";import{useState}from"react";import{useRouter}from"next/navigation";import{createClient}from"../../lib/supabase/client";export default function Login(){const r=useRouter(),s=createClient();const[email,setEmail]=useState(""),[password,setPassword]=useState(""),[loading,setLoading]=useState(false),[error,setError]=useState("");async function login(e:React.FormEvent){e.preventDefault();setLoading(true);setError("");const{error}=await s.auth.signInWithPassword({email,password});if(error){setError(error.message);setLoading(false);return}r.push("/jobs");r.refresh()}async function passkey(){setLoading(true);setError("");const{error}=await s.auth.signInWithPasskey();if(error){setError(error.message);setLoading(false);return}r.push("/");r.refresh()}return <main className="shell"><section className="panel narrow"><div className="brand">SOLVIX <span>OS</span></div><p className="eyebrow" style={{marginTop:30}}>SECURE ACCESS · ZERO-TRUST ENTRY</p><h1 className="title">Enter Solvix securely.</h1><p className="muted">Authenticate first. Your workspace opens securely, while jobs and their paychecks remain visible before any job billing is requested.</p><div className="stack" style={{marginTop:24}}><button className="btn primary" onClick={passkey} disabled={loading}>⌁ SIGN IN WITH PASSKEY / FACE ID</button><div className="row muted"><span>────────</span><span className="mono" style={{fontSize:10}}>OR CREDENTIALS</span><span>────────</span></div><form onSubmit={login} className="stack"><input className="input" type="email" placeholder="secure@email.com" value={email} onChange={e=>setEmail(e.target.value)} required/><input className="input" type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)} required/><button className="btn primary" disabled={loading}>{loading?"AUTHENTICATING…":"ENTER SECURE WORKSPACE →"}</button></form>{error&&<div className="error">{error}</div>}</div></section></main>}