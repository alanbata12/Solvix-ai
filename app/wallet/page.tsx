"use client";
import { useEffect,useState } from "react"; import { createClient } from "../../lib/supabase/client";
export default function WalletPage(){const [email,setEmail]=useState("");useEffect(()=>{createClient().auth.getUser().then(({data})=>setEmail(data.user?.email||""))},[]);
return <main className="min-h-screen bg-[#050508] text-white p-4 md:p-8"><div className="max-w-xl mx-auto"><a href="/dashboard" className="text-sm text-gray-400">← Dashboard</a><section className="mt-6 p-6 rounded-3xl bg-white/5 border border-white/10"><p className="text-xs font-mono text-gray-500 tracking-widest">SOLVIX WALLET</p><h1 className="text-3xl font-bold mt-2">Fund Wallet</h1><p className="text-gray-400 mt-3">Add funds through a supported payment rail. Funds are credited only after provider confirmation.</p><div className="mt-6 p-4 rounded-2xl bg-black/40 border border-white/10"><div className="text-xs text-gray-500">ACCOUNT</div><div className="mt-1">{email||"Authenticated session required"}</div></div><a href="https://jvmicoqonhuuyedvpmcn.supabase.co/functions/v1/solvix-deposit" target="_blank" rel="noreferrer" className="block mt-6 w-full text-center rounded-xl bg-white text-black font-bold py-4">OPEN SECURE DEPOSIT →</a><p className="text-xs text-gray-600 mt-5">The existing Solvix card-funding endpoint is currently disabled; this page does not pretend a deposit succeeded.</p></section></div></main>}
